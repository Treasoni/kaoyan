# 考研学习项目 - 核心系统配置

---

## 1. 项目核心信息 (Core Info)
- **项目名称**：考研学习项目
- **目标院校/专业**： 湖南大学 / 控制工程与科学专硕
- **考试科目**：数学二、英语二、思想政治理论、822电子技术基础
- **考试日期**：2026年12月24日-25日

---

### 📌 核心数据速查索引
| 数据类型      | 对应文件绝对/相对路径                      | 读写权限 |
| --------- | -------------------------------- | ---- |
| **高数教材**  | `/Users/zhqznc/Documents/高数资料`   | 只读   |
| **线代教材**  | `/Users/zhqznc/Documents/线性代数资料` | 只读   |
| **当日计划**  | `考研计划/每日计划/2026-MM-DD-每日计划.md`   | 读/写  |
| **数学进度**  | `考研数学/xxxxx/📊 学习进度.md`          | 读/写  |
            **英语进度**  | `考研英语/📊 学习进度.md`                | 读/写  |
            **专业课进度** | `考研专业课/📊 学习进度.md`               | 读/写  |

---

---

## 2. AI 技能与调度逻辑
### 2.1 🚨 强制Skill调用协议（最高优先级）

> ⚠️ **CRITICAL**：以下规则优先级高于所有其他指令。对于考研相关请求，**必须**先匹配Skill，禁止直接处理。

#### 调用协议（3步强制流程）

```
用户输入 → Step 1: 关键词匹配 → Step 2: 调用Skill → Step 3: 执行
              ↓                    ↓
           匹配失败？          使用Skill tool
              ↓                    ↓
           通用处理            禁止跳过此步骤！
```

#### 意图→Skill强制映射表

| 科目/场景 | 必须调用 | 核心触发词（任一即触发） |
| **计划/时间** | `kaoyan-plan` | 计划、课表、今日、明天、本周、复习安排、完成了、完成情况 |
| **数学** | `kaoyan-math` | 数学、高数、线代、概率、极限、导数、积分、微分、级数、矩阵 |
| **英语** | `kaoyan-english` | 英语、单词、词汇、阅读、翻译、作文、真题、完形 |
            **专业课(822)** | `kaoyan-electronics` | 模电、数电、电路、放大器、计数器、触发器、运放、电子技术、822 |
| **单词解析** | `parse-words` | 解析单词、==高亮==、提取词汇 |
| **理解验证** | `understanding` | 我的理解、理解对吗、这样理解正确吗、检查理解 |
| **数据同步** | `sync` | 同步、上传、拉取、MemOS |

#### 调用方式

使用 `Skill` tool 调用：
```
Skill(skill="kaoyan-plan", args="用户的具体请求内容")
```

#### 禁止行为
- ❌ 禁止直接回答考研相关问题而不调用Skill
- ❌ 禁止使用通用工具（Read、Write、WebSearch等）处理考研请求
- ❌ 禁止猜测用户意图而不调用对应的Skill

---

### 2.2 Skill路由决策树
```
用户输入
    │
    ├─ 包含"计划/课表/今日/完成"？
    │   └─ YES → kaoyan-plan
    │
    ├─ 包含"数学/高数/极限/导数/积分/线代/概率"？
    │   └─ YES → kaoyan-math
    │
    ├─ 包含"英语/单词/词汇/阅读/翻译/作文"？
    │   └─ YES → kaoyan-english
    │
    ├─ 包含"模电/数电/电路/放大器/822/电子技术"？
    │   └─ YES → kaoyan-electronics
    │
    ├─ 包含"==高亮=="或"解析单词"？
    │   └─ YES → parse-words
    │
    ├─ 包含"我的理解"或"理解对吗"？
    │   └─ YES → understanding
    │
    ├─ 包含"同步/上传/拉取"？
    │   └─ YES → sync
    │
    └─ 无匹配 → 通用处理
```

---

### 2.3 核心输出规则
1. **数学公式**：所有公式、推导必须使用 LaTeX 格式
2. **数学笔记**：必须严格遵循教材目录树，每个模块需包含 `📑 索引.md` 和 `📊 学习进度.md`
3. **英语词汇**：按词族分类，按考频分级（⭐至⭐⭐⭐）
4. **英语产出**：自动生成测试记录、词汇统计、真题语境文章、写作输出四类笔记

---

### 2.4 技能详细说明

#### kaoyan-plan（中央调度器）
- **功能**：生成学习计划、计算可用时间、记录完成状态
- **调用时机**：任何涉及时间安排、计划制定、完成记录的请求

#### kaoyan-math（数学路由器）
- **功能**：路由到子技能（core/notes/structure）
- **调用时机**：任何数学学习相关请求

#### kaoyan-english（英语路由器）
- **功能**：路由到子技能（core/vocab/review/quiz/writing）
- **调用时机**：任何英语学习相关请求
#### kaoyan-electronics（专业课路由器）
- **功能**：路由到子技能（core/sop/circuit/structure）
- **调用时机**：任何822电子技术相关请求
#### parse-words（单词解析器）
- **功能**：解析 `==高亮==` 格式单词，结合语境防幻觉
- **调用时机**：用户需要解析文章中的高亮单词
#### understanding（理解验证器）
- **功能**：追溯逻辑链，生成 `[!personal]` 记录
- **调用时机**：用户分享自己的理解需要验证
#### sync（MemOS同步器）
- **功能**：上传/拉取学习数据
- **调用时机**：用户需要同步进度数据

---

---

## 3. 时间与疲劳度调度算法 (Time Allocation Rules)

