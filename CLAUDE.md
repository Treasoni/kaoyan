# 考研学习项目 - 核心系统配置

## 1. 🔧 Obsidian 工作流分工 (Workflow Division)

**强制执行** ： **必须**严格遵循以下分工模式：

### 1.1 查找与读写 → obsidian-cli
- **用途**：快速定位文件路径、批量操作、属性查询
- **典型场景**：
  - "找到所有包含 `#错题` 标签的笔记"
  - "列出本周的所有每日计划文件"
  - "搜索某个知识点的相关笔记"
- **示例命令**：
  ```bash
  # 搜索包含特定标签的笔记
  obsidian search "tag:错题"

  # 列出每日计划文件
  obsidian list "考研计划/每日计划"
  ```

### 1.2 内容生成与重构 → Claude Code
- **用途**：深度分析、逻辑检查、内容重写、跨文件整合
- **典型场景**：
  - "帮我检查 `./考研计划/每日计划/2026-03-27.md` 里的逻辑错误并修正"
  - "重构这节笔记的结构，让知识点更清晰"
  - "把这段内容整理成标准格式"
- **优势**：直接在终端对话，Claude 可读取、分析、修改文件

### 1.3 最佳实践搭配 (Best Practice)

| 场景 | 推荐工具 | 原因 |
|------|----------|------|
| **管理结构** | obsidian-cli | 创建新笔记、移动分类、修改文件名 — CLI 了解 Obsidian 路径规则 |
| **处理内容** | Claude Code | 编辑改写、润色、语义查找 — Claude 理解内容含义 |

**语义查达示例**（只记得内容，不记得文件名）：
```
用户："我之前写过一篇关于极限的笔记，里面提到过夹逼定理"
Claude：用 Grep 搜索关键词 → 找到匹配文件 → 返回给用户
```

### 1.4 协作模式
```
用户需求 → 先用 obsidian-cli 定位文件 → 告诉 Claude 去处理它
```
**示例流程**：
1. 用户："帮我整理今天的错题"
2. Claude：先用 obsidian-cli 找到当日计划文件 → 提取错题 → 调用 `/mistake-book` 整理

### 1.5 obsidian-cli 不可用时的替代方案

当 `obsidian-cli` 无法连接到 Obsidian 主进程时（常见原因：Obsidian 未运行、版本过旧、CLI 插件未启用），**自动切换**到以下替代方案：

| obsidian-cli 命令 | 替代方案 (Glob + Grep + Read) |
|------------------|------------------------------|
| `obsidian search "tag:错题"` | `Grep pattern="#错题" output_mode=files_with_matches` |
| `obsidian list "考研计划/每日计划"` | `Glob pattern="考研计划/每日计划/*.md"` |
| `obsidian get "path/file.md"` | `Read file_path="path/file.md"` |
| `obsidian get "path.md" --frontmatter` | `Read file_path="path.md"` (读取后解析 YAML) |

**错误检测**：
```bash
# 当返回以下错误时，自动切换到替代方案
# "Unable to connect to main process"
# "Your Obsidian installer is out of date"
```



---

## 2. 项目核心信息 (Core Info)
- **项目名称**：考研学习项目
- **目标院校/专业**：湖南大学 / 控制工程与科学专硕
- **考试科目**：数学二、英语二、思想政治理论、822电子技术基础
- **考试日期**：2026年12月24日-25日

### 📌 核心数据速查索引
| 数据类型      | 对应文件绝对/相对路径                      | 读写权限 |
| --------- | -------------------------------- | ---- |
| **高数教材**  | `/Users/zhqznc/Documents/高数资料`   | 只读   |
| **线代教材**  | `/Users/zhqznc/Documents/线性代数资料` | 只读   |
| **当日计划**  | `考研计划/每日计划/2026-MM-DD-每日计划.md`   | 读/写  |
| **数学进度**  | `考研数学/高数-极限与连续/📊 学习进度.md`       | 读/写  |
| **英语进度**  | `考研英语/📊 学习进度.md`                | 读/写  |
| **专业课进度** | `考研专业课/📊 学习进度.md`               | 读/写  |

---

## 3. AI 技能与调度逻辑 (Skills & Routing)

### 3.1 技能架构
- **`/kaoyan-plan`**：中央调度器（生成计划、计算可用时间、记录完成状态）
- **`/kaoyan-math`**：路由器（下辖 `core` / `notes` / `structure` / `summary`）
- **`/chapter-summary`**：章节总结生成器（整理数学和专业课章节笔记、生成📝章节总结.md）
- **`/kaoyan-english`**：路由器（下辖 `core` / `vocab` / `review` / `quiz` / `writing`）
- **`/parse-words`**：单词解析器（解析 `==高亮==` 单词，结合语境防幻觉，增量插入源文件）
- **`/kaoyan-electronics`**：专业课处理器
- **`/understanding`**：理解验证器（追溯逻辑链，强制要求补全推导，生成 `[!personal]` 记录）
- **`/mistake-book`**：错题整理器（快速整理错题到各科目错题本，自动格式化、更新索引、追加内容）
- **`/sync [拉取/上传]`**：MemOS 数据同步器

### 3.2 核心输出规则
1. **数学公式**：所有公式、推导必须使用 LaTeX 格式。
2. **数学笔记**：必须严格遵循教材目录树（独立子文件夹如 `0-基础知识/`），每个模块需包含 `📑 索引.md` 和 `📊 学习进度.md`。
3. **英语产出**：对单词表按词族分类，按考频分级（⭐至⭐⭐⭐）整理，自动生成测试记录、词汇统计、真题语境文章、写作输出四类笔记。
4. **技能优先**：禁止使用通用宽泛命令，必须优先调用上述定制技能。
5. **章节总结**：当用户要求"章节总结"、"整理笔记"、"汇总内容"时，**必须优先调用 `/chapter-summary`**，而非手动规划。
6. **错题整理**：当用户要求"整理错题"、"记错题"、"把这道题放到错题本"时，**必须优先调用 `/mistake-book`**，而非手动编辑。

---

## 4. 时间与疲劳度调度算法 (Time Allocation Rules)

制定 `/kaoyan-plan` 时，必须严格遵循以下算法与约束：

### 4.1 最小时间块约束
- **数学（中高难度）**：>= 1.5小时（严禁碎片化）
- **英语阅读 / 专业课 / 数学（概念）**：>= 1.0小时
- **单词 / 政治选择题**：可碎片化（15-20分钟）

### 4.2 疲劳度自动降级机制
疲劳度计算公式：`Fatigue = 主观感受(0.6) + 行为数据(0.4)`
- **精力很好 (Fatigue < 0.3)**：正常排期，休息占比 15%
- **正常 (Fatigue 0.3-0.5)**：正常排期，休息占比 20%
- **有点累 (Fatigue 0.6-0.8)**：削减 10%-20% 任务量，休息占比 25%
- **很累 (Fatigue > 0.8)**：削减 30%+ 任务量，强制开启长休息/休息日，休息占比 30%+

### 4.3 时段优先级分配
- **上午** (08:00-12:00)：数学、英语单词
- **下午** (14:00-18:00)：英语阅读、专业课
- **晚上** (19:00-23:00)：专业课、政治复盘
- **深夜** (23:00后)：仅复习，禁止排期新内容

---

## 5. 每日标准工作流 (Daily Workflow)

1. **Start** -> 执行 `/sync 拉取` 获取最新进度与错题。
2. **Plan** -> 执行 `/kaoyan-plan` 结合当前疲劳度与可用时间生成课表。
3. **Study** -> 调用各科目技能（`/kaoyan-math`, `/kaoyan-english` 等）执行学习。
4. **Verify** -> 学完重要节点，输入图片/文字并执行 `/understanding` 验证逻辑链。
5. **Update** -> 反馈 `/kaoyan-plan 我完成了...`，触发日志创建与各科目 `📊 学习进度.md` 的更新。
6. **End** -> 执行 `/sync 上传` 将数据与错题落盘至 MemOS。

---

## 6. 🛡️ 数据安全与保护边界 (Protected Blocks)
**严禁修改、覆盖或删除**以下区块及包含此类特征的文件内容，更新时必须作追加处理或跳过：
- `## 个人笔记` / `## My Insights`
- `## 随手记` / `## Quick Notes`
- `## 学习心得` / `## Learnings`
- `## 踩坑记录` / `## Pitfalls`
- `## 待探索` / `## TODO`
- 任何以 `> [!personal]` 开头的 callout 块
