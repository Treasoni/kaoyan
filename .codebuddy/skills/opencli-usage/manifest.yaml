apiVersion: agent-platform/v1
kind: Skill
metadata:
  name: opencli-usage
  version: 0.1.0
  description: "Use at the start of any OpenCLI session — this is the top-level map of what `opencli` can do, how to discover adapters, what flags and output formats are universal, and which specialized skill to load next. Point here wh"
spec:
  entrypoint: SKILL.md
  capabilities:
    - skill.opencli-usage
  permissions:
    filesystem: write
    network: allow
    subprocess: none
    git: none
  dependsOn: []
  lifecycle:
    discoverable: true
    deprecated: false
