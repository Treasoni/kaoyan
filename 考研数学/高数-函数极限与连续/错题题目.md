# 高数-函数极限与连续 · 错题题目

## 错题01

函数 $f(x) = \frac{e^x + xe^x}{e^x - 1} - \frac{1}{x}$，若 $f(x)$ 在 $x=0$ 处连续，则应补充 $f(0) = \underline{\quad}$。

---

## 错题02

已知 $\lim_{x\to 0}\frac{f(x)}{x^2}$ 存在，且函数 $f(x) = \frac{x - \sin x}{x} + x^{2} \lim_{x \to 0} \frac{f(x)}{1 - \cos x}$，则 $\lim_{x\to 0}\frac{f(x)}{x^2} = (\quad)$

(A) $-\frac{1}{3}$　(B) $\frac{1}{3}$　(C) $\frac{1}{6}$　(D) $-\frac{1}{6}$

---

## 错题03

设函数 $f(x)$ 在 $(-\infty, +\infty)$ 内有定义，在区间 $[0, 2]$ 上，$f(x) = x(x^2 - 4)$。若对任意 $x$ 都满足 $f(x) = -\frac{1}{2}f(x+2)$，求 $f(x)$ 在 $[-2, 0]$ 上的表达式。

---

## 错题04

设函数 $f(x)$ 在 $(-\infty, +\infty)$ 上满足 $f(x) = f(x - \pi) + \sin x$。���知当 $x \in [0, \pi)$ 时，$f(x) = x$。求 $f(x)$ 在 $[\pi, 3\pi)$ 上的表达式。

---

## 错题05

计算极限
$$\lim_{x \to +\infty} \ln(1+2^x) \ln\left(1+\frac{2}{x}\right)$$

---

## 错题06

设 $f(x) = \frac{\sin x}{1 + x^2}$ 在 $x = 0$ 处的 $3$ 次泰勒多项式为 $ax + bx^2 + cx^3$，求 $a, b, c$。

(A) $a=1, b=0, c=-\frac{7}{6}$

---

## 错题07

设 $f(x) = \lim_{n\to \infty}\frac{1 + x}{1 + x^{2n}}$，求该函数的间断点。

(B) 存在间断点 $x = 1$

---

## 错题08

画出函数 $f(x) = \arcsin(\sin x)$ 的图形。

---

## 错题09

设 $y = \sin x, x \in [0, 2\pi]$，求其在各单调区间上的反函数。

---

## 错题10

设函数 $f(x), g(x)$ 在 $x=0$ 的某去心邻域内有定义且不为零。若当 $x \to 0$ 时，$f(x)$ 是 $g(x)$ 的高阶无穷小，则当 $x \to 0$ 时，下列结论正确的个数为（ ）

① $f(x) + g(x) = o(f(x))$
② $f(x) \cdot g(x) = o(g^2(x))$
③ $f(x) = o(\ln[1 + g(x)])$
④ $f(x) = o(g^2(x))$

(A) 1　(B) 2　(C) 3　(D) 4

---

## 错题11

设 $f(x) = \lim_{t \to \infty} \frac{x + e^{tx}}{1 + e^{tx}}$，则 $x=0$ 是 $f(x)$ 的（    ）。

---

## 错题12

设曲线 $y = f(x) = x^n$ 在点 $(1, 1)$ 处的切线与 $x$ 轴的交点为 $(\xi_n, 0)$，求 $\displaystyle\lim_{n \to \infty} f(\xi_n)$。
