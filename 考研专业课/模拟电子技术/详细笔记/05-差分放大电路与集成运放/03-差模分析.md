---
type: note
tags:
  - 模拟电子技术
  - 822
  - 模电
  - 知识点笔记
chapter: 05-差分放大电路与集成运放
created: 2026-07-12
last_updated: 2026-09-01
parent: [[考研专业课/模拟电子技术/详细笔记/05-差分放大电路与集成运放/00-章节导读|章节导读]]
source_note: [[考研专业课/模拟电子技术/详细笔记/05-差分放大电路与集成运放|整章版]]
---

# 差模分析

> [!info] 学习定位
> 差模分析决定有用信号增益。

- 上级：[[考研专业课/模拟电子技术/详细笔记/05-差分放大电路与集成运放/00-章节导读|章节导读]]
- 整章：[[考研专业课/模拟电子技术/详细笔记/05-差分放大电路与集成运放|整章版]]
- 上一节：[[考研专业课/模拟电子技术/详细笔记/05-差分放大电路与集成运放/02-差分放大核心思想|差分放大核心思想]]
- 下一节：[[考研专业课/模拟电子技术/详细笔记/05-差分放大电路与集成运放/04-共模分析与CMRR|共模分析与 CMRR]]

---

## 4. 差模分析

### 4.1 差模时公共支路为什么是交流地

![[考研专业课/模拟电子技术/附件/05-差分放大电路与集成运放/图7-2-3ab-差模输入交流通路.jpg]]

差模输入时：

$$
v_{i1}=+\frac{v_{id}}{2},\qquad v_{i2}=-\frac{v_{id}}{2}
$$

两边产生大小相等、方向相反的漏极小信号电流：

$$
i_{d1}=g_mv_{gs1},\qquad i_{d2}=g_mv_{gs2}
$$

且

$$
i_{d1}=-i_{d2}
$$

流入公共源极节点的交流电流相互抵消，因此尾电流源动态电阻 $r_o$ 中没有交流电流。公共源极节点交流电压为 0，可视为交流地。

这就是差分对最重要的结论：

```text
差模输入时，公共源极/发射极节点交流接地。
```

### 4.2 MOS 差分对差模增益

若忽略 $r_{ds}$，且 $R_{d1}=R_{d2}=R_d$。

单端输出，从 $v_{o1}$ 取：

$$
A_{vd1}
=\frac{v_{o1}}{v_{id}}
=-\frac{1}{2}g_mR_d
$$

单端输出，从 $v_{o2}$ 取：

$$
A_{vd2}
=\frac{v_{o2}}{v_{id}}
=+\frac{1}{2}g_mR_d
$$

双端输出：

$$
A_{vd}
=\frac{v_{o1}-v_{o2}}{v_{id}}
=-g_mR_d
$$

所以：

```text
双端输出差模增益 = 单端输出差模增益的 2 倍。
```

### 4.3 双端输出带负载

![[考研专业课/模拟电子技术/附件/05-差分放大电路与集成运放/图7-2-3c-双端输出带负载.jpg]]

负载 $R_L$ 跨接在两个输出端之间。差模时两端输出大小相等、相位相反，所以 $R_L$ 中点是交流地。对单边而言，负载等效为：

$$
\frac{R_L}{2}
$$

双端差模增益变为：

$$
A_{vd}=-g_m\left(R_d\parallel\frac{R_L}{2}\right)
$$

> [!warning] 负载等效最容易错
> 双端输出的 $R_L$ 不是直接和 $R_d$ 并联，而是单边等效为 $R_L/2$。只有单端输出到地接负载时，才是 $R_d\parallel R_L$。

### 4.4 单端输入并不等于只有差模

![[考研专业课/模拟电子技术/附件/05-差分放大电路与集成运放/图7-2-4-单端输入等效.jpg]]

若 $v_{i1}=v_{id}$，$v_{i2}=0$，这叫单端输入。它可以等效为：

$$
v_{ic}=\frac{v_{id}}{2}
$$

$$
\frac{v_{id}}{2},\quad -\frac{v_{id}}{2}
$$

也就是说，单端输入包含：

1. 一个差模分量。
2. 一个共模分量。

不过若题目只问差模增益，单端输入的差模分析与双端输入相同。

### 4.5 BJT 差分对的半电路法

![[考研专业课/模拟电子技术/附件/05-差分放大电路与集成运放/图-差分放大-差模共模半电路对照.png]]

> [!note] 图的作用
> 左下是差模半电路：对称面为虚地，尾阻不进入差模增益分母；右下是共模半电路：两管电流相加，尾阻折算为 $2R_E$。做题先看输入关系，再选对应半电路。

图中把同一个对称 BJT 差分对拆成“差模半电路”和“共模半电路”，用于快速写增益。设每个基极串联电阻为 $R_b$，晶体管小信号基极—发射极电阻为 $r_{be}$，集电极电阻为 $R_C$，尾部电阻为 $R_E$；忽略晶体管的 $r_o$。

差模激励时：

$$
v_{i1}=+\frac{v_{id}}{2},\qquad
v_{i2}=-\frac{v_{id}}{2}
$$

两管发射极电流的增量大小相等、方向相反，公共发射极节点为交流地。因此，对单端输出 $v_{o1}$：

$$
\frac{v_{o1}}{v_{i1}}
=-\frac{\beta R_C}{R_b+r_{be}}
$$

若题目以差分输入 $v_{id}$ 为分母，则还要乘输入分配的 $1/2$：

$$
A_{vd1}=\frac{v_{o1}}{v_{id}}
=-\frac{\beta R_C}{2(R_b+r_{be})}
$$

双端输出定义为 $v_{od}=v_{o1}-v_{o2}$ 时：

$$
A_{vd}=\frac{v_{od}}{v_{id}}
=-\frac{\beta R_C}{R_b+r_{be}}
$$

若输入源为理想电压源，令 $R_b=0$；若存在集电极负载或有限 $r_o$，将 $R_C$ 换成相应的交流等效负载。

> [!warning] $2R_E$ 到底放在哪里
> 对称差模时，公共发射极节点是交流地，尾部电阻 $R_E$ 不出现在上述差模增益分母中。只有共模半电路才把实际尾部电阻折算为 $2R_E$：一侧晶体管的发射极电流流过的是全电路的尾电阻，而全尾电流等于两侧电流之和。手写图中把 $2R_E$ 画在“差模”标题附近，按电路条件应改读为共模半电路；这是本页最需要纠正的标注。

> [!example]- 手写来源（2026-08-26，第 3 页）
> 该页对应“差分对 → 交流通路 → 半电路等效 → 增益”的推导；其中红色 $2R_E$ 的位置按上方警告修正。
>
> ![[考研专业课/模拟电子技术/附件/05-差分放大电路与集成运放/手写-差分放大-03-差模与共模半电路-2026-08-26.jpg]]

### 4.6 任意双输入：共模与差模叠加

![[考研专业课/模拟电子技术/附件/05-差分放大电路与集成运放/图-差分放大例题-静态与任意输入分解.png]]

> [!note] 图的作用
> 任意两个输入都能唯一拆成“同向变化的共模分量”和“反向变化的差模分量”。先做分解，再分别求两种激励下的输出，最后利用线性叠加合成总输出；图中把正负号、分量大小和输出组合放在同一张图里，避免把单端输入误当成纯差模。

统一定义

$$
u_{id}=u_{i1}-u_{i2},\qquad
u_{ic}=\frac{u_{i1}+u_{i2}}{2}
$$

反解得到两个输入端的实际电压：

$$
u_{i1}=u_{ic}+\frac{u_{id}}{2},\qquad
u_{i2}=u_{ic}-\frac{u_{id}}{2}
$$

因此，任意双输入可以按下面的顺序处理：

1. **只保留共模分量**：令 $u_{id}=0$，即 $u_{i1}=u_{i2}=u_{ic}$，求共模输出 $u_{oc}=A_{uc}u_{ic}$。
2. **只保留差模分量**：令 $u_{ic}=0$，即 $u_{i1}=u_{id}/2$、$u_{i2}=-u_{id}/2$，求差模输出 $u_{od}=A_{ud}u_{id}$。
3. **叠加两种响应**：对同一个输出端口，写成

$$
u_o=A_{uc}u_{ic}+A_{ud}u_{id}
$$

其中 $A_{uc}$、$A_{ud}$ 必须针对同一种输出定义。若采用双端输出
$u_{od}=u_{o1}-u_{o2}$，则上式中的 $u_o$ 应统一换成 $u_{od}$；若采用单端输出，则 $u_o$ 取某一侧对地电压。

对理想对称差分放大器，双端输出的共模分量完全抵消，故

$$
A_{uc}\approx0,\qquad
u_{od}\approx A_{ud}u_{id}
$$

单端输出通常不能完全抵消共模分量，仍需保留 $A_{uc}u_{ic}$。此外，$A_{ud}$ 的数值取决于输出口：以单端输出为分母时，常比双端差模增益小一半；以双端输出为分母时，需按 $u_{od}=u_{o1}-u_{o2}$ 的极性统一符号。

> [!warning] 输入分解与输出定义必须成对出现
> 不能只写“差模增益”而不说明输出端。先写清 $u_{id}$、$u_{ic}$ 的定义，再注明是单端 $u_o$ 还是双端 $u_{od}$，最后代入相应的 $A_{ud}$、$A_{uc}$。若把 $u_{i1}$、$u_{i2}$ 的正负号或双端输出极性换掉，增益符号也要随之改变。

> [!example]- 手写来源（2026-09-01，第 2 页）
> 该页给出了任意双输入拆分为 $(u_{i1}+u_{i2})/2$ 与 $\pm(u_{i1}-u_{i2})/2$ 的画法，并分别讨论共模、差模激励。
>
> ![[考研专业课/模拟电子技术/附件/05-差分放大电路与集成运放/手写-差分放大例题-02-输入分解-2026-09-01.jpg]]

> [!example]- 手写来源（2026-09-01，第 3 页）
> 该页把两种分量的输出相加，得到“共模增益 × 共模输入 + 差模增益 × 差模输入”的总响应。
>
> ![[考研专业课/模拟电子技术/附件/05-差分放大电路与集成运放/手写-差分放大例题-03-输出叠加-2026-09-01.jpg]]

---

---

## 相关笔记

- 上一节：[[考研专业课/模拟电子技术/详细笔记/05-差分放大电路与集成运放/02-差分放大核心思想|差分放大核心思想]]
- 下一节：[[考研专业课/模拟电子技术/详细笔记/05-差分放大电路与集成运放/04-共模分析与CMRR|共模分析与 CMRR]]
