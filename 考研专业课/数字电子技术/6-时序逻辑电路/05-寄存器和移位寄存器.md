---
创建日期: 2026-04-28
tags:
  - 数字电子技术
  - 时序逻辑电路
  - 寄存器
  - 移位寄存器
  - 考研专业课
---

# 05 寄存器和移位寄存器

## 一、寄存器

### 基本概念

- 寄存器用于**存储二进制数据**
- 1 个触发器存 1 位，$N$ 位数据需 $N$ 个触发器
- 本质是**最简单的同步时序电路**

### 典型芯片：74HC/HCT374（8位寄存器）

- CP 上升沿 → $D_7 \sim D_0$ 同时存入
- $\overline{OE} = 0$ → 通过三态门并行输出 $Q_7 \sim Q_0$
- 属于**脉冲边沿敏感**电路

### 寄存器 vs 锁存器

| 特征 | 锁存器（如74HC373） | 寄存器（如74HC374） |
|------|-------------------|-------------------|
| 敏感方式 | 电平敏感 | 脉冲边沿敏感 |
| 同步性 | 输出不一定同时更新 | 输出同时更新 |
| 适用场景 | 数据更新可能在控制信号有效后出现 | 数据在 CP 触发沿前已稳定 |

> [!tip] 选择原则
> - 数据更新可能在控制信号开始有效之后 → 用**锁存器**
> - 数据在 CP 触发沿前已稳定且要求同时更新 → 用**寄存器**

## 二、移位寄存器

### 1. 基本移位寄存器

由 $N$ 个 D 触发器级联构成，左边触发器输出作为右边触发器的输入。

**4 位移位寄存器功能表**：

| CP | $Q_0$ | $Q_1$ | $Q_2$ | $Q_3$ |
|:--:|:-----:|:-----:|:-----:|:-----:|
| 第1个CP前 | × | × | × | × |
| 1 | $D_3$ | × | × | × |
| 2 | $D_2$ | $D_3$ | × | × |
| 3 | $D_1$ | $D_2$ | $D_3$ | × |
| 4 | $D_0$ | $D_1$ | $D_2$ | $D_3$ |

**关键参数**：
- 串入→并出：需 $N \cdot T_{CP}$
- 并出→串出：需 $N \cdot T_{CP}$

> [!important]
> 移位寄存器**只能用脉冲边沿敏感的触发器**，不能用锁存器（锁存器在CP高电平期间输出跟随输入变化，移位操作无法控制）。

### 2. 多功能双向移位寄存器

**控制功能**（由 $S_1S_0$ 选择）：

| $S_1$ | $S_0$ | 功能 |
|:-----:|:-----:|------|
| 0 | 0 | 保持 |
| 0 | 1 | 右移 |
| 1 | 0 | 左移 |
| 1 | 1 | 并行输入 |

> [!note] 移位方向定义
> - **右移**：数据从低位触发器移向高位（LSB→MSB）
> - **左移**：数据从高位触发器移向低位（MSB→LSB）

### 3. 典型芯片：74HC/HCT194（4位双向移位寄存器）

| 引脚 | 功能 |
|------|------|
| $D_{SR}$ | 右移串行数据输入 |
| $D_{SL}$ | 左移串行数据输入 |
| $D_{I0} \sim D_{I3}$ | 并行数据输入 |
| $Q_0 \sim Q_3$ | 并行输出 |
| $\overline{CR}$ | 异步清零（低电平有效） |
| $S_1, S_0$ | 工作模式选择 |
| CP | 时钟脉冲 |

**功能表**：

| $\overline{CR}$ | $S_1$ | $S_0$ | CP | 功能 |
|:---:|:---:|:---:|:---:|------|
| L | × | × | × | 异步清零 |
| H | L | L | × | 保持 |
| H | L | H | ↑ | 右移（$D_{SR}$ → $Q_0$） |
| H | H | L | ↑ | 左移（$D_{SL}$ → $Q_3$） |
| H | H | H | ↑ | 同步并行置数 |

## 三、移位寄存器的应用

### 1. 串并转换
- 串行输入 → 经过 $N$ 个 CP → 并行输出
- 并行置入 → 经过 $N$ 个 CP → 串行输出

### 2. 延时
- 数据经过 $N$ 个 CP 才从输出端出现
- 延迟时间 = $N \times T_{CP}$

### 3. 乘除运算
- **左移 1 位** = 乘以 2
- **右移 1 位** = 除以 2
- 左移 $n$ 位 = $\times 2^n$，右移 $n$ 位 = $\div 2^n$
