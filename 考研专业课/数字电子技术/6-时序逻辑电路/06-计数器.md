---
创建日期: 2026-04-28
tags:
  - 数字电子技术
  - 时序逻辑电路
  - 计数器
  - 考研专业课
---

# 06 计数器

> [!abstract] 概述
> 计数器是最常用的时序电路，功能包括：**计数、分频、定时、产生节拍脉冲**。

## 一、计数器的分类

| 分类依据 | 类型 |
|---------|------|
| 触发器动作 | 同步计数器、异步计数器 |
| 数值增减 | 递增计数器、递减计数器、可逆计数器 |
| 编码方式 | 二进制、BCD、循环码 |
| 模数 | 模 $n$（$n$ 进制） |

**模 $M$**：计数器一个计数周期内的状态总数。

## 二、二进制计数器

### 1. 异步二进制计数器

**结构**：$N$ 个 $T'$ 触发器级联，前级 $Q$ 作为后级时钟

**4 位异步二进制递增计数器**：
- 由 4 个下降沿触发 $T'$ 触发器组成
- 模 $M = 2^4 = 16$
- $Q_0$：2 分频，$Q_1$：4 分频，$Q_2$：8 分频，$Q_3$：16 分频

> [!warning] 异步计数器的缺点
> - 各触发器逐级翻转（"纹波进位"），从 0111→1000 时经过 0110→0100→0000 几个中间状态
> - 存在**纹波延迟**，总延迟 = $N \times t_{pd}$
> - 需满足 $T_{CP} \gg Nt_{pd}$

### 2. 同步二进制计数器

**激励方程**（用 T 触发器实现 $N$ 位递增计数器）：

$$T_0 = 1$$

$$T_i = Q_{i-1}Q_{i-2}\cdots Q_1 Q_0 = \prod_{j=0}^{i-1}Q_j \quad (i = 1, 2, \dots, N-1)$$

> [!important] 规律
> 第 $i$ 位翻转条件：**所有低位都为 1**

**递减计数器激励方程**：

$$T_0 = 1, \quad T_i = \bar{Q}_{i-1}\bar{Q}_{i-2}\cdots\bar{Q}_1\bar{Q}_0 = \prod_{j=0}^{i-1}\bar{Q}_j$$

> 第 $i$ 位翻转条件：**所有低位都为 0**

#### 进位方式

| 方式 | 特点 |
|------|------|
| **串行进位** | 延迟大（$t_{COMpd(max)} = (N-2)t_{Gpd}$），电路简单 |
| **并行进位** | 延迟小（$t_{COMpd(max)} = t_{Gpd}$），但扇入扇出增大 |
| **并串结合** | 组内并行、组间串行，折中方案 |

### 3. 典型集成计数器：74LVC161

**功能**：4 位同步二进制递增计数器

| 引脚 | 功能 | 有效电平 |
|------|------|---------|
| $\overline{CR}$ | 异步清零 | 低电平有效 |
| $\overline{PE}$ | 同步并行置数 | 低电平有效 |
| CEP, CET | 计数使能 | 高电平有效 |
| CP | 时钟 | 上升沿 |
| $D_3 \sim D_0$ | 并行数据输入 | - |
| $Q_3 \sim Q_0$ | 并行输出 | - |
| TC | 进位输出 | $Q_3Q_2Q_1Q_0 \cdot CET$ |

**功能表**：

| $\overline{CR}$ | $\overline{PE}$ | CEP | CET | CP | 功能 |
|:---:|:---:|:---:|:---:|:---:|------|
| L | × | × | × | × | 异步清零 |
| H | L | × | × | ↑ | 同步置数 |
| H | H | L | × | × | 保持 |
| H | H | × | L | × | 保持（TC=0） |
| H | H | H | H | ↑ | 计数 |

> [!tip] 161系列对比
> | 型号 | 进制 | 清零方式 |
> |------|------|---------|
> | 74LVC161 | 二进制（模16） | **异步**清零 |
> | 74LVC163 | 二进制（模16） | **同步**清零 |
> | 74LVC160 | BCD（模10） | **异步**清零 |
> | 74LVC162 | BCD（模10） | **同步**清零 |

## 三、用集成计数器构成任意模数计数器

### 方法1：反馈清零法

**适用**：有清零输入端的计数器

**步骤**：
1. 确定模数 $N$
2. **异步清零**：状态 $S_N$ 译码 → 反馈到 $\overline{CR}$
3. **同步清零**：状态 $S_{N-1}$ 译码 → 反馈到 $\overline{CR}$

> [!warning] 异步清零 vs 同步清零
> - **异步清零**：到达 $S_N$ 状态**��即**清零，$S_N$ 瞬间出现（窄脉冲）
> - **同步清零**：到达 $S_{N-1}$ 状态后，等待**下一个 CP 沿**才清零

**例：用 74LVC161（异步清零）构成模 9 计数器**

- $S_N = S_9 = 1001$
- $\overline{CR} = \overline{Q_3 Q_0}$
- 有效状态：0000~1000（1001 瞬间出现后立即清零）

### 方法2：反馈置数法

**适用**：有预置数功能的计数器

**步骤**：
1. 选择起始状态
2. 用目标状态译码控制 $\overline{PE}$
3. 数据输入端接起始状态值

**例：用 74LVC161（同步置数）构成模 9 计数器**

方案一：从 0000 开始
- $S_{N-1} = S_8 = 1000$，$\overline{PE} = \overline{Q_3}$
- $D_3D_2D_1D_0 = 0000$

方案二：利用进位输出
- $TC$ 反馈到 $\overline{PE}$，$D_3D_2D_1D_0 = 0111$
- 计数范围：0111~1111

### 方法3：多片级联（$M < N$ 的情况）

**例：用两片 BCD 计数器构成模 24 计数器**

- 低位片：个位（模 10）
- 高位片：十位（模 10）
- 当输出为 0010 0100（即 24）时，整体反馈清零

## 四、环形计数器

### 1. 基本环形计数器

- 将移位寄存器**最高位输出**反馈到**最低位输入**
- $N$ 个触发器 → 模 $N$ 计数器
- 不需译码即可直接输出状态信号
- **缺点**：状态利用率低（$N$ 个触发器只有 $N$ 个有效状态）

### 2. 扭环形计数器（约翰逊计数器）

- 将移位寄存器**最高位取反**反馈到**最低位输入**
- $N$ 个触发器 → 模 $2N$ 计数器（状态数翻倍）
- 译码简单，每次只有一个触发器翻转，**无竞争-冒险**
- 典型芯片：74HC4017（模 10 扭环形计数器）

> [!important] 环形 vs 扭环形
> | 特征 | 环形计数器 | 扭环形计数器 |
> |------|----------|------------|
> | 反馈方式 | $Q_{N-1} \to D_0$ | $\bar{Q}_{N-1} \to D_0$ |
> | 模数 | $N$ | $2N$ |
> | 状态利用率 | 低 | 较高 |
> | 译码难度 | 无需译码 | 简单（每个状态只需2变量） |
